version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
    deploy:
      resources:
        limits:
          cpus: "0.50"
          memory: 512M
